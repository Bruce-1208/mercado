# 1688 连接与搜图修复验收记录

版本：1.8.18

## 修复内容

- 连接缺少接收端时，在有界等待期间逐框架重新注入；避免一次性注入全部框架被无权限 iframe 阻断。
- 页面导航导致消息通道关闭时，不重复发送上传动作，交由结果页检查流程处理。
- 新文档未安装内容脚本时直接恢复接收端，不额外刷新首页。
- 版本标记存在但监听器失效时重新注册；同一有效监听器不会重复注册。
- 识别上轮真实截图中出现的“以图搜款”入口，并等待延迟加载的入口和上传控件。

## 本轮自动化检查

命令：`python3 -m pytest -q tests/test_zeshun_popup_browser.py tests/test_zeshun_browser_extension.py tests/test_ai_weight_price_client.py`

结果：106 passed，37.51 秒。另通过两个 JavaScript 文件的 node --check 和 git diff --check。

针对性覆盖：连续三次接收端缺失后恢复且仅上传一次、连接等待截止、导航后结果页恢复、不刷新新结果页、失效监听器恢复、异步“以图搜款”入口上传和点击、既有两组各 20 次上传搜索模拟流程。

这些是自动化模拟页面回归，不是登录状态下 20 个真实商品的验收。

## 部署与真实页面限制

文件已同步到上轮在 Edge 扩展管理页确认的实际加载目录 `/Users/a11/Downloads/zeshun_collector 5/`，安装包为 `zeshun-collector-extension-1.8.18.zip`。

本轮尚未在 Edge 中确认重新加载 1.8.18。上轮 Computer Use 在当前 1688 URL 上明确拒绝继续并终止会话，本轮未绕过该限制。已请求用户切到插件或扩展管理页面，以便继续核对扩展版本。真实 1688 上传、搜索结果及回填仍未验收通过。
