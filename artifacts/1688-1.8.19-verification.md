# 1.8.19 已上传图片弹层搜索修复

用户截图可确认：图片上传已完成，局部弹层显示“以图搜款 / 已上传1张图”，底部存在“搜索”按钮。截图无法确认 DOM 中的 input.files 或图片 URL，因此这两者作为故障复现条件，而非已经从真实页面读取到的事实。

旧版复现：模拟网络预览图片、上传后清空文件控件时，返回“1688主图已上传，但未找到可执行的图片搜索按钮”，搜索次数为 0。

修复：识别有上传数量、预览图、以图搜款标题的最小局部弹层，不再依赖文件控件保留文件或预览使用 blob/data URL；排除首页关键词输入区域。合并按钮及内部文字的重复候选，等待提交控件最多 15 秒。

验证：针对性搜图测试 11 项通过，其中清空、替换、移除输入框三种情形各连续完成 20 次模拟上传/搜索/结果读取，首页搜索误点均为 0。

完整回归最后一轮为 108 passed、2 failed；失败来自旧重量断言与工作区已有的 SKU 平均重量行为不一致。校正断言后定向复测两项均通过；未修改重量计算代码。两份 JavaScript 通过 node --check，变更通过 git diff --check。

已同步真实扩展加载目录 `/Users/a11/Downloads/zeshun_collector 5/` 并打包 `zeshun-collector-extension-1.8.19.zip`。运行中的 Edge 扩展尚未确认重新加载为 1.8.19。

真实 1688 页面可视化操作仍受 Computer Use URL 限制，本轮未访问或绕过限制；模拟测试不代表 20 个真实商品验收通过。
