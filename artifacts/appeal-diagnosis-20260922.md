# 自动化申诉：最近 24 小时报错诊断与修复

统计窗口：北京时间 2026-09-21 21:08:43 至 2026-09-22 21:08:43。

读取当前服务端的 Agent 队列 `~/Library/Application Support/Zeshun/MercadoWorkbench/local-agent-hub.sqlite3`（只读）及 `.data/workbench-server.err.log`。共检查 5,920 条 Agent 事件，涉及 14 个任务。`bit/logs` 下的旧日志未作为本次窗口的统计来源。分类按明确的报错行计数，同一轮可能对应多个事件；不将 traceback 中重复的异常文本再计为一次业务失败。结构化统计见同目录 `appeal-diagnosis-20260922.json`。

## 发现与处理

| 现象 | 窗口内数量 | 原因及处理 |
| --- | ---: | --- |
| 整轮 `Forbidden` | 355 | 旧 Agent 1.2.0 的任务 `5ceb60406334a4fc5cec357a2ea59f7b` 已被服务端标为租约过期，远端进程仍每 180 秒循环。该 Agent 已无运行中的日常任务，数据库访问被正确拒绝；旧停止列表却不包含已结束任务。已让心跳继续下发已领取且终止的任务 ID，并让新的日常任务在 401/403 时退出，不恢复失效任务权限。示例事件 73384、78599。 |
| 找不到 AI 客服 iframe | 199 | 失败快照存在只有 Help 内容和 Hotjar iframe、没有助手的页面。站点切换代码确实会在缺少菜单时搜索整个文档、点击隐藏选项、重复点击一次选项。已收窄目标并确保单击。4 个离线 Chromium 场景均能使旧代码失败，新代码全部通过。此修复不能证明全部 199 次故障同源，真实平台未加载助手或网络异常仍需远端验证。示例事件 75544。 |
| 浏览器容量等待超时 | 65 | 日常任务默认请求 15 个进程，原调度上限 30，但窗口准入默认只有 3 个。多余进程在等待期间耗尽 180 秒预算。已让调度并发取任务上限与窗口上限的较小值，保留原有内存保护；手动占用窗口或其他任务仍可能消耗可用容量。示例事件 75607。 |
| 整轮 HTTP 500 / 502 | 12 / 6 | 500 的服务端堆栈落在数据库连接/游标；同窗口有 773 行 MySQL 1040（连接数耗尽），但不能将每个 500 都直接等同于 1040。502 为代理返回的网关页面，部分与服务重启时间重合。已为只读请求增加最多 3 次的有限重试，写入与创建任务不重放。持续数据库或网关故障仍需服务端治理。示例事件 78303、75296、78592。 |
| Agent 缺少 `dbutils` | 1 | 客户端导入整个工作台时，顶层加载了只应由数据库服务端使用的 DBUtils。已改为按需导入，并验证 Agent 缺少此包且禁止联网时仍可导入。示例事件 73746。 |
| 登录失败 / 页面导航失败 | 21 / 16 | 涉及平台登录未完成、renderer 超时及连接重置。保留登录熔断和发送确认规则，未把这类异常改判成功。 |

另有 892 行站点校验未匹配记录，其中包含切换前探测和轮询，不等于 892 次最终申诉失败。`sent_unknown` 仍表示发送结果无法确认，不能为消除日志而自动重发。

旧发布版本还曾因导入核重核价模块、访问其无权访问的 API 而启动失败；当前源码已有 Agent 跳过该迁移的修复，本次隔离导入测试同时覆盖此行为，未扩大 Agent 权限。

## 验证

- 最终相关回归：**237 passed, 1 skipped**。跳过项依赖 Windows 的固定 Edge 路径；本次新增的 4 项 Chromium 页面测试已实际执行。
- 旧 `select_mercado_site_fast` 用完全相同的 4 个页面场景执行，得到 **4 failed**；修复后 **4 passed**，覆盖无菜单、隐藏选项、正常选项和先展开菜单的场景。
- 覆盖 GET 的 500/502/503/504 和读超时恢复、重试上限、POST 不重放、HTML 401/403 识别、轮询/日常循环鉴权退出、过期任务下发停止、窗口并发限制及客户端缺少 DBUtils 的启动过程。
- 停止下发还通过实际 Flask 心跳路由和临时 SQLite 队列验证；没有修改真实任务状态。
- 在线只读检查：MySQL 当时为 9 个连接、上限 151；健康接口正常，随后采样的服务进程连接池为 0 活跃、4 空闲、上限 12。数据库已在检查前重启，因此这些数字只说明采样时恢复，不能证明历史连接耗尽已永久解决。
- Python 编译和 `git diff --check` 通过。

测试命令：

```sh
BIT_BACKGROUND_SERVICES_DISABLED=1 python3 -m pytest -q -rs \
  tests/test_appeal_recent_failures.py tests/test_local_agent_hub.py \
  tests/test_local_agent_interface.py tests/test_bit_appeal_ai_messages.py \
  tests/test_bit_appeal_stability.py tests/test_workbench_runtime_interface.py \
  tests/test_bit_daily_task.py tests/test_bit_browser_lifecycle.py
```

## 生效范围与剩余验证

本次完成源码修复和隔离测试，没有向真实客服发送测试申诉。服务端关闭热更新，已运行的 Windows worker 使用各自 release 目录，源码变化不会替换这些进程。

生效顺序：先在统一部署窗口重启服务端以加载新的停止下发逻辑；旧失控任务应在下一次心跳收到停止 ID。远端更新 Agent，并停止旧任务、重新创建任务，使 worker 加载最新业务包。无需为测试批量重放之前发送结果不确定的申诉。

本次没有重启生产服务或正在运行的远端任务：检查期间工作区还有其他并行进行的数据库连接池修改，因此没有把这些未在本任务内审核的改动一起部署。数据库连接池文件及其他任务的改动均予以保留。后续需要在真实远端核对首次站点切换、聊天输入框加载、发送回显与任务停止；当前不能宣称线上报错已全部消失。
